import { type ZodType, z } from "zod/v4";
import type {{schemaName}} from "../schemas/{{schemaName}}";

export const schemaOf{{schemaName}} = {{{zodSchema}}} as unknown as ZodType<{{schemaName}}, {{schemaName}}>;
