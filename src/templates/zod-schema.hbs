import { z } from "zod/v4";
{{#each dependencies}}
import { schemaOf{{this}} as {{this}} } from "./{{this}}";
{{/each}}

export type TypeOf{{schemaName}} = {{{zodType}}};

export const schemaOf{{schemaName}} = {{{zodSchema}}} as unknown as TypeOf{{schemaName}};
