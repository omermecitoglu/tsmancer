import { type ZodType, type z } from "{{zodSource}}";

export function parseZodSchema<T extends ZodType>(schema: T, data: z.infer<T>) {
  const result = schema.safeParse(data);
  if (!result.success) throw result.error;
  return result.data;
}
